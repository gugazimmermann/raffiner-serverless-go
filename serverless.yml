service: raffiner

provider:
  name: aws
  runtime: go1.x

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  clients:
    handler: bin/main
    events:
      - http:
          path: users
          method: get
      - http:
          path: users
          method: post
      - http:
          path: users
          method: put
      - http:
          path: users
          method: delete